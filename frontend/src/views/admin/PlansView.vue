<script setup lang="ts">
/**
 * Placeholder — Plans (master data)
 * TODO: CRUD plans (code, name, price_vnd, features JSON, is_active).
 *       Form tạo/sửa + table list với filter active/inactive.
 * BE ready: POST /plans, PATCH /plans/:id, DELETE /plans/:id, GET /plans?includeInactive=true.
 * Store: stores/plan.ts đã có sẵn adminState.adminPlans + createPlan/updatePlan/deletePlan.
 */
</script>

<template>
  <div class="p-6">
    <h1 class="text-2xl font-semibold text-gray-900">Gói dịch vụ</h1>
    <p class="mt-2 text-sm text-gray-500">CRUD plans — đang phát triển.</p>
  </div>
</template>
